#!/bin/zsh

antibody update
antibody bundle < "${ZDOTDIR}/plugins" > "${ZDOTDIR}/plugins.zsh"
echo "Compiled: "
for file in $(find "${ZDOTDIR}" -type f | grep -P '.*(?<!\.zwc)$'); do
	zcompile "$file"
	echo "$file"
done
